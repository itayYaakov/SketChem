export enum Direction {
    Top = 1,
    Bottom,
    Left,
    Right,
}

export enum MouseButtons {
    None = 0,
    Left = 1,
    Right = 2,
    Scroll = 4,
    Back = 8,
    Forward = 16,
}

export enum BondType {
    Single = 1,
    Double,
    Triple,
    WedgeFront,
    WedgeBack,
    Aromatic,
    Any,
}
